name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: node ${{ matrix.node }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        node: [18, 20, 22]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      # No install step: the runtime has no dependencies, and that is a
      # property worth noticing if it ever stops being true.
      - name: Check for dependencies
        shell: bash
        run: |
          if [ -f package-lock.json ] || [ -d node_modules ]; then
            echo "Pēdāg has no dependencies; something added one."
            exit 1
          fi

      - name: Version is consistent
        shell: bash
        run: |
          pkg=$(node -p "require('./package.json').version")
          cli=$(node bin/Pēdāg.mjs --version)
          if [ "$pkg" != "$cli" ]; then
            echo "package.json says $pkg, the CLI says $cli"
            exit 1
          fi
          grep -q "\[$pkg\]" CHANGELOG.md || { echo "no changelog entry for $pkg"; exit 1; }

      - name: Test
        run: node --test tests/*.test.mjs

      # `std/` was once missing from the package's file list, so an installed
      # copy had no standard library while every local test passed. Only a real
      # install catches packaging.
      - name: The published package works from a clean install
        shell: bash
        run: |
          npm pack --pack-destination "$RUNNER_TEMP"
          mkdir -p "$RUNNER_TEMP/install-check"
          cd "$RUNNER_TEMP/install-check"
          npm init -y > /dev/null
          npm install "$RUNNER_TEMP"/pedag-*.tgz
          printf 'import "std/math" as math\nprint(math.mean([2, 4, 6]))\n' > hello.pedag
          test "$(npx Pēdāg run hello.pedag)" = "4"

      - name: The standard library's own tests
        run: node bin/Pēdāg.mjs test std/

      - name: Everything is formatted
        run: node bin/Pēdāg.mjs fmt examples/ --check

      - name: Run the examples
        shell: bash
        run: |
          node bin/Pēdāg.mjs run examples/tour.pedag
          node bin/Pēdāg.mjs run examples/quantum.pedag
          node bin/Pēdāg.mjs run examples/agents.pedag
          node bin/Pēdāg.mjs run examples/devices.pedag
          node bin/Pēdāg.mjs run examples/evolve.pedag
          node bin/Pēdāg.mjs run examples/modern.pedag
          node bin/Pēdāg.mjs run examples/typed.pedag
          node bin/Pēdāg.mjs run examples/crypto.pedag --grant crypto
          node bin/Pēdāg.mjs run examples/interop.pedag --grant ffi

      - name: Every example must type-check clean
        shell: bash
        run: |
          # The gradual guarantee is only worth something if it holds on real
          # code. agents.pedag is excluded: it contains a deliberate race.
          for f in tour quantum devices evolve modern typed crypto contracts; do
            node bin/Pēdāg.mjs check "examples/$f.pedag"
          done

      - name: The verifier must still refute the known-bad contracts
        shell: bash
        run: |
          # examples/contracts.pedag contains deliberately false contracts, so a
          # clean verify would mean the prover stopped working.
          if node bin/Pēdāg.mjs verify examples/contracts.pedag; then
            echo "verify proved a file containing known-false contracts"
            exit 1
          fi

      - name: Contracts must still hold where they are supposed to
        shell: bash
        run: |
          # examples/contracts.pedag ships deliberate counterexamples, so a
          # zero exit here would mean the prover stopped finding them.
          if node bin/Pēdāg.mjs prove examples/contracts.pedag; then
            echo "prove found nothing in a file that contains known-bad contracts"
            exit 1
          fi

      - name: A bundle must run standalone
        shell: bash
        run: |
          node bin/Pēdāg.mjs build examples/tour.pedag -o ci-tour.mjs
          node ci-tour.mjs > ci-bundled.txt
          node bin/Pēdāg.mjs run examples/tour.pedag > ci-direct.txt
          diff ci-bundled.txt ci-direct.txt
          rm -f ci-tour.mjs ci-bundled.txt ci-direct.txt

      - name: The race checker must still see the planted race
        shell: bash
        run: |
          if node bin/Pēdāg.mjs check examples/agents.pedag; then
            echo "check found no race in a file that contains one"
            exit 1
          fi
